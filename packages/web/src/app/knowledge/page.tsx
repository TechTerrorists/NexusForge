import { Database, ShieldCheck } from "lucide-react";

export default function KnowledgePreviewPage() {
  return <div className="mission-page"><header className="mission-heading"><div><p className="eyebrow">Preview surface</p><h1>Knowledge</h1><p>Repository context currently flows through explicit artifacts and dependency summaries. A tenant-scoped retrieval workspace will appear here after ingestion, provenance, and prompt-injection isolation are fully wired.</p></div><span className="badge amber">Preview</span></header><section className="panel empty-state min-h-[420px]"><div><Database size={30} className="mx-auto mb-4" style={{ color: "var(--purple-4)" }} /><h2 className="text-base font-semibold">No simulated knowledge bases</h2><p className="mt-2 max-w-lg" style={{ color: "var(--fg-muted)" }}>This area intentionally shows no fake documents or metrics. Role retrieval is live under Workforce; document ingestion remains unavailable.</p><span className="signal-chip mt-5"><ShieldCheck size={12} /> Truthful preview state</span></div></section></div>;
}
